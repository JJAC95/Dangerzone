import { Button } from "@mui/material";
import { SetRepWeight } from "./SetRepWeight";
export const ExercisePage = ({ navigation, route }) => {
  console.log("nav", navigation, route);
  return <SetRepWeight />;
};
